import styled from 'styled-components';

const Title = styled.h1`
  display: inline-block;
  font-family: 'Bangers', cursive;
  font-size: 2.8rem;
  text-align: center;
  margin: 10px;
  letter-spacing: 3px;
  padding: 5px 10px;
  text-shadow: 4px 3px 0px var(--secondary-color);
`;

export default Title;
