import styled from 'styled-components';

const Link = styled.a`
  color: black;
  text-decoration: none;
  margin-left: 5px;
  font-weight: bold;
  border-bottom: 5px solid var(--secondary-color);
`;

export default Link;
